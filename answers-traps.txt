(1) 在a0-a7中存放参数，13存放在a2中
(2) 在C语言中(call.c),main 调用 f 然后 f 再调用 g。而在call.asm中发生了函数的内联。
    因为没有找到jal 或者 jalr 指令跳转到f 和 g 函数。而是通过在mian函数中 li	a1,12 代码，计算出f(8)+1，并存储
(3) 1092行代码 0000000000000630 <printf>: 说明printf地址在0000000000000630
(4)   30:	00000097          	auipc	ra,0x0
  34:	600080e7          	jalr	1536(ra) # 630 <printf>
  exit(0);
  38:	4501                	li	a0,0
  jalr指令会把下一条指令的地址存在ra寄存器里。在执行printf时，指令地址为0x34，那么下一条指令的地址为0x38
  所以ra寄存器存的值为0x38
(5) 输出结果为HE110 World
    57616=0xE110, 0x00646c72小端存储为72-6c-64-00，对照ASCII码表  72:r 6c:l 64:d 00:充当字符串结尾标识
    若为大端存储，i应改为0x726c6400，不需改变57616
(6) y= 之后会打印出一个不可预测的值
    printf 遇到了两个 %d 格式化符号，因此它会去读取 a1 和 a2 寄存器。参数 3 被放入了 a1 寄存器对应第一个 %d；
    由于没有提供第三个参数，a2 寄存器没有被重新赋值。printf 会直接读取调用发生那一刻 a2 寄存器中残留数据并将其打印出来。
