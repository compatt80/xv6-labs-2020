通过阅读 notxv6/ph.c 可以发现，其中的put()代码通过 key % NBUCKET 最为哈希函数
insert()通过头插法插入新的冲突哈希节点
假设有两个线程T1、T2，他们同时运行到insert的e->next = n;代码
此时两个进程看到的旧链表表头都是n，此时，如果T1线程先执行*p = e;
T2线程后执行*p = e; 则T1线程插入的节点会被覆盖掉。所有此时A申请的节点没有加入哈希表
此后访问哈希表时就会出现key missing

而一个线程肚独自运行不会被覆盖掉。